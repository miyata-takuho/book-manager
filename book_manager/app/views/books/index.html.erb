<h1>登録書籍</h1>
  <div class="main posts-index">
    <div class="container">
      <% @books.each do |book| %>
        <div class="posts-index-item">
          <%= link_to(book.title, "#{book.id}") %>
        </div>
      <% end %>
    </div>
  </div>
